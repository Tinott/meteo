# ============================================================
#  -Qrad/(dtheta/dz) * dudz_tot  (colonnes seches pour Qrad)  vs  terme_flux, en (z,t), 2-8km
# ============================================================
w_sec_rce_x_shear = w_sec_rce_s * dudz_tot

fig, axes = plt.subplots(1,2,figsize=(11,5),sharey=True)
v = max(np.percentile(np.abs(w_sec_rce_x_shear[m28]),98), np.percentile(np.abs(terme_flux[m28]),98))*3600+1e-12
for ax,(fld,lab) in zip(axes,[(w_sec_rce_x_shear*3600, r"$-\frac{\dot Q_{rad}}{\partial_z\bar\theta}\,\partial_z\bar u$ (m/s/h)"),
                                (terme_flux*3600, r"$-\frac{1}{\rho_0}\partial_z(\rho_0\overline{u'w'})$ (m/s/h)")]):
    im=ax.pcolormesh(tt+t_stat, zkm[m28], fld[m28], cmap='RdBu_r',
                      norm=TwoSlopeNorm(0,-v,v), shading='auto')
    ax.set_xlabel('t'); ax.set_title(lab,fontsize=10)
    fig.colorbar(im,ax=ax,pad=.02)
axes[0].set_ylabel('z (km)')
plt.suptitle("Fermeture radiative-subsidente complete vs terme de flux reel (2-8km)", fontweight='bold')
plt.tight_layout(); plt.show()
