dx = float(x[1]-x[0]); dz = float(z[1]-z[0])
rhoU = rho0[:,None]*U; rhoW = rho0[:,None]*W
div  = np.gradient(rhoU, dx, axis=1) + np.gradient(rhoW, dz, axis=0)
rot  = np.gradient(rhoW, dx, axis=1) - np.gradient(rhoU, dz, axis=0)
print("‖div‖ / ‖rot‖ =", np.linalg.norm(div)/np.linalg.norm(rot))
