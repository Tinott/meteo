# ============================================================
#  12 diagrammes de Hovmoller : TENDANCE (T1 et T2), humide/sec x up/down/env
#  -- vmax UNIQUE, commune aux 12 panneaux (comparaison directe possible)
# ============================================================
rows = [
    ('tend(T1)', 'humide', tend_T1_cat['humide']),
    ('tend(T1)', 'sec',    tend_T1_cat['sec']),
    ('tend(T2)', 'humide', tend_T2_cat['humide']),
    ('tend(T2)', 'sec',    tend_T2_cat['sec']),
]

# vmax commun : max (98e percentile) sur les 12 champs a la fois
vmax = max(
    np.nanpercentile(np.abs(cat_dict[c][mask_show]) * 3600, 98)
    for _, _, cat_dict in rows
    for c in cats_names
) + 1e-12

fig, axes = plt.subplots(4, 3, figsize=(15, 16), sharex=True, sharey=True)

for i, (label, rname, cat_dict) in enumerate(rows):
    for j, cname in enumerate(cats_names):
        ax = axes[i, j]
        fld = cat_dict[cname][mask_show] * 3600   # m/s -> m/s/h
        im = ax.pcolormesh(tt+t_stat, zkm[mask_show], fld, cmap='RdBu_r',
                            norm=TwoSlopeNorm(0, -vmax, vmax), shading='auto')
        ax.set_title(f"{label} — {rname} — {cname}", fontsize=10)
        if i == 3:
            ax.set_xlabel('t')
        if j == 0:
            ax.set_ylabel('z (km)')

# une seule colorbar partagee, a droite de toute la figure
fig.subplots_adjust(right=0.9)
cbar_ax = fig.add_axes([0.92, 0.15, 0.015, 0.7])
fig.colorbar(im, cax=cbar_ax, label='m/s/h')

plt.suptitle("Hovmoller de la tendance : -(1/rho0)*dz(T1) et -(1/rho0)*dz(T2) — humide/sec — up/down/env (echelle commune)",
             fontweight='bold', y=1.01)
plt.show()
