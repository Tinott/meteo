# pour un niveau iz, sur la region :
up = w_r > ws
# contribution updraft vs downdraft au flux de Reynolds VRAI
flux_up = (w_p[up]   * u_p[up]).sum()   / N_tot   # w+ u'
flux_dn = (w_p[~up]  * u_p[~up]).sum()  / N_tot   # w- u'  (manque dans Mc)
# top-hat (ce que fait le flux de masse) :
Mc_loc   = (w_p[up]).mean() * frac_up
tophat   = Mc_loc * (u_r[up].mean() - u_r[~up].mean())
print(flux_up, flux_dn, tophat, flux_up - tophat)
