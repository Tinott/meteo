# --- champ à spectraliser : w organisé (panaches), PAS la vorticité ---
champ = worg_all                                  # (nz_D, n_x) -- déjà localisé sur les updrafts
champ_reg = np.empty((nz_D, n_x))
for i in range(n_x):
    champ_reg[:, i] = np.interp(z_reg, z_D, champ[:, i])
dzr = float(z_reg[1] - z_reg[0])

# pondère par rho0 pour ne pas surpondérer la haute troposphère (densité faible)
champ_reg *= rho0_full[:, None] / rho0_full[0]

Zhat = np.fft.fft2(champ_reg)                      # <-- remplace zeta_reg
