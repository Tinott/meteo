---------------------------------------------------------------------------
ValueError                                Traceback (most recent call last)
Cell In[14], line 6
      4     _step  = idx_stat.step if idx_stat.step is not None else 1
      5     if idx_stat.stop is None:
----> 6         raise ValueError(
      7             "idx_stat est un slice sans 'stop' defini (ex: slice(10, None)) -- "
      8             "impossible de le convertir automatiquement en tableau. "
      9             "Remplace-le par slice(10, 110) ou par un tableau explicite avant de continuer."
     10         )
     11     idx_stat = np.arange(_start, idx_stat.stop, _step)
     12 else:

ValueError: idx_stat est un slice sans 'stop' defini (ex: slice(10, None)) -- impossible de le convertir automatiquement en tableau. Remplace-le par slice(10, 110) ou par un tableau explicite avant de co
