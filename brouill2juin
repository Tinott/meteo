a = xi / sd            # pentes en unités physiques
b = yv.mean() - np.sum(xi * mu / sd)   # ordonnée à l'origine
for k, coef in zip(names, a):
    if abs(coef) > 1e-12:
        print(f'{k:20s}  a = {coef:+.4e}')
print(f'intercept b = {b:+.4e}')
