# --- Restriction verticale 2–18 km ---
    ZMIN, ZMAX = 2000., 18000.        # en mètres (z = alt est en m)
    kz = (z >= ZMIN) & (z <= ZMAX)
    z    = z[kz]
    rho0 = rho0[kz]
    U    = U[kz];   W = W[kz];   UPWP = UPWP[kz]
    ubar = ubar[kz];  wbar = wbar[kz]
    upwp = UPWP
    n_z  = z.size
    print(f'Restreint à {z[0]:.0f}–{z[-1]:.0f} m : {n_z} niveaux')





kz = (z >= 2000.) & (z <= 18000.)
    z, rho0 = z[kz], rho0[kz]
    U, W, upwp = U[kz], W[kz], upwp[kz]
    ubar, wbar = ubar[kz], wbar[kz]
    n_z = z.size
