fig, ax = plt.subplots(figsize=(4, 6))
ax.plot(rho0_full[kz], alt[kz]/1000, 'k-', lw=1.5)
ax.set_xlabel(r'$\rho_0$ [kg/m$^3$]')
ax.set_ylabel('z [km]')
ax.set_title('Densité de référence $\\rho_0(z)$')
ax.grid(alpha=0.3)
plt.tight_layout(); plt.show()
