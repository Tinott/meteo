print(ds['ua'].dims)          # confirme l'ordre exact
print(ds[dim_z].values[:5])   # premieres valeurs de la coordonnee z
print(ds[dim_z].values[-5:])  # dernieres valeurs
print(ds[dim_z].attrs)        # unites/nom, si renseignes
print(list(ds.coords))        # toutes les coordonnees disponibles du fichier
