# AVANT : half_x = max(3, int(0.5*(lam_x_dom*1000)/dxl))   # = λx/2 -> avale rouge+bleu
# APRÈS : un vortex = demi-longueur d'onde, demi-largeur = λx/4
half_x = max(3, int(0.25*(lam_x_dom*1000)/dxl))
