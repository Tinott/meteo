# ============================================================
#  wbar * dudz(ubar) — termes 100% domaine-total (pas de masque humide/sec),
#  affiche sur 2-8km uniquement
# ============================================================
wbar_smooth = smooth_t(wbar_zt)
produit_domtot = wbar_smooth * dudz_tot

fig, ax = plt.subplots(figsize=(8,5)); ax.grid(False)
v = np.percentile(np.abs(produit_domtot[m28]), 98)*3600 + 1e-12
im = ax.pcolormesh(tt+t_stat, zkm[m28], produit_domtot[m28]*3600, cmap='RdBu_r',
                    norm=TwoSlopeNorm(0,-v,v), shading='auto')
ax.set_xlabel('t'); ax.set_ylabel('z (km)')
ax.set_title(r"$\bar w\,\partial_z\bar u$ (m/s/h), domaine total, 2-8km")
fig.colorbar(im, ax=ax, pad=.02, label='m/s/h')
plt.tight_layout(); plt.show()
