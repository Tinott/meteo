s = slice(None)   # ou ta bande
num_T1 = np.sum(T1tot[s] * phi2d[s])      # projection de T1 sur Phi
num_T2 = np.sum(T2tot[s] * phi2d[s])
den    = np.sum(phi2d[s]**2)
print(f"part T1 = {num_T1/den:.0%}   part T2 = {num_T2/den:.0%}   (somme = 100%)")
