j0 = k_max
plt.figure(figsize=(7,4))
plt.semilogx(lam_km[1:], kx[1:]*Co_T2[j0,1:], 'C3')   # k*Co vs lambda
plt.gca().invert_xaxis()
plt.axhline(0, color='grey', lw=.6)
plt.xlabel(r'$\lambda$ [km]'); plt.ylabel(r'$k\,\mathrm{Co}$')
plt.title(f'Co-spectre area-preserving, z={z_D[j0]/1000:.1f} km')
plt.show()
