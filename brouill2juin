contrib_a = a * rho0[s]*sig_w[s]**2
contrib_c = c * rho0[s]*sig_u[s]*sig_w[s]
print('contribution médiane terme a:', np.nanmedian(np.abs(contrib_a)))
print('contribution médiane terme c:', np.nanmedian(np.abs(contrib_c)))
