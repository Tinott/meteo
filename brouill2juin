# A quel indice temporel debute vraiment l'etat stationnaire utilise pour calculer mh_flat/ms_flat ?
print("idx_stat va de", idx_stat.min(), "a", idx_stat.max(), "- longueur:", len(idx_stat))
# cherche la definition du "dernier tiers" utilisee pour calculer le seuil PRW / mh_flat / ms_flat
