w_all = ds_w['wa'].isel({dim_t: idx_stat, dim_z: iz_test}).values.reshape(n_stat, -1)  # SANS masque region

for it in [20, 25, 30, 50, 80]:
    ww = w_all[it, :]
    print(f"t={it:3d}  wmax={ww.max():.2f}  wmin={ww.min():.2f}  std={ww.std():.3f}")


files = path3d('wa')  # ou la fonction qui liste les fichiers
print(len(files) if hasattr(files, '__len__') else "verifier ce que path3d retourne")
print(ds_w[dim_t].values[20:35])   # les vraies valeurs de temps (pas les indices), autour de la coupure
print(ds_w[dim_t].values[-5:])     # la fin du fichier
